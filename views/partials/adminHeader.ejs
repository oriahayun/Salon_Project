<header id="nav-wrapper">
    <nav id="nav">
        <div class="nav left">
            <span class="gradient skew">
                <h1 class="logo un-skew">
                    <a href="/admin-dashboard">
                        BeautySN
                    </a>
                </h1>
            </span>
            <button id="menu" class="btn-nav"><span class="fas fa-bars"></span></button>
        </div>
        <div class="nav right">
            <a href="/admin-dashboard" <% if (pageName === 'admin-dashboard') { %>class="nav-link active" <% } else { %>class="nav-link" <% } %>>
                <span class="nav-link-span">
                    <span class="u-nav">Home</span>
                </span>
            </a>
            <a href="/users" <% if (pageName === 'users') { %>class="nav-link active" <% } else { %>class="nav-link" <% } %>>
                <span class="nav-link-span">
                    <span class="u-nav">Users</span>
                </span>
            </a>
            
            <a href="/logout" class="nav-link">
                <span class="nav-link-span">
                    <span class="u-nav">Logout</span>
                </span>
            </a>
        </div>
    </nav>
</header>